.row.justify-content-center
  .col-md-8
    .card.border-success
      .card-header.bg-success.text-white
        %h4.mb-0
          %i.bi.bi-check-circle.me-2
          Two-Factor Authentication Enabled!
      .card-body
        .alert.alert-success.border-0
          %i.bi.bi-shield-check.me-2
          Your account is now protected with two-factor authentication.
        
        .alert.alert-warning
          %i.bi.bi-exclamation-triangle.me-2
          %strong Important:
          Save these backup codes in a secure location. Each code can only be used once.
        
        %h6 Your Backup Codes:
        .row
          - @backup_codes.each_slice(5) do |codes|
            .col-md-6
              .card.bg-light.mb-3
                .card-body
                  - codes.each do |code|
                    %div
                      %code.fs-6= code
        
        .alert.alert-info
          %i.bi.bi-info-circle.me-2
          %strong What are backup codes?
          If you lose access to your authenticator app, you can use these backup codes 
          to sign in to your account. Store them somewhere safe!
        
        .text-center.mt-4
          = link_to "Continue to Profile", manage_profile_path, class: "btn btn-primary btn-lg me-3"
          = link_to "Print Backup Codes", "#", class: "btn btn-outline-secondary", onclick: "window.print(); return false;"

:css
  @media print {
    .btn, .alert:not(.alert-warning), .card-header {
      display: none !important;
    }
    
    .card {
      border: none !important;
      box-shadow: none !important;
    }
    
    h6 {
      font-size: 18px !important;
      margin-bottom: 20px !important;
    }
    
    code {
      font-size: 16px !important;
      font-weight: bold !important;
    }
  }
