- content_for :title, @post.title

= render @post

.container-fluid.px-0
  .row
    .col-lg-8.mx-auto.px-4
      %hr{style: "margin: 6rem 0;"}
      .text-center
        = link_to posts_path(@back_to_posts_page ? { page: @back_to_posts_page } : {}), class: 'btn btn-primary' do
          %i.bi.bi-arrow-left.me-2
          Back to Posts
